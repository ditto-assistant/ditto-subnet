interface:
  display_name: "Backroom Review"
  short_description: "Review SN118 quarantines and high-score ATH holds"
  default_prompt: "Use $backroom-review to review the active SN118 quarantine queue and high-scoring leaderboard, then apply audited release, reject, clear, or hold decisions."

dependencies:
  tools:
    - type: "mcp"
      value: "sn118-backroom"
      description: "OAuth-protected SN118 Backroom operator MCP"
      transport: "streamable_http"
      url: "https://backroom.dittobench.ai/mcp"

policy:
  allow_implicit_invocation: true
