---
id: recall-v1
agent_id: f93297ad-6701-4f22-baab-6b3c88427e1c
agent_name: recall-v1
agent_version: 1
resolution: reject
tags: [i7, i5, i3, tool-planning, family-compiler, independent-invariants]
holding: Trained catalog swap plus next_required is I7; family calculation sheets are independently I5. Grep of WORKED OUT / remainder names is not an I5 pass.
---

Never-reviewed champion. Policy-v10 stamp with `deferred-mechanical-admission`.
SHA `e4af3e27…`. Live ATH `5c1c390a` rejected I7 only and recorded I4/I5 as
not the basis after those names grepped clean.

I7 is enough: `predict_relevant_tools` (`tool_classifier.py:280`, used at
`baseline.py:1902`) is a trained head; `host_tools` replaces the live catalog
(`baseline.py:1962`); `required_tool_names` plus `next_required`
(`loop.py:661`) expose only the host-selected next function. Pass clauses
`no_tool_planning`, `policy_capability_filter_only`, and
`natural_singleton_class` are refuted.

I5 still fails independently: `pipeline/flow.py` classifies
calculation/chronology/comparison/lesson/suggestion families, and
`baseline.py` selects calculation, net-change, literal-guidance,
historical-scope, and minor-unit instruction sheets. Those are closed
operand/sign/output recipes. Zero hits on `WORKED OUT` / remainder-sheet
names is a search lead, not an I5 keep.

I3 also fails: host semantic checks discard a later model review and serve
the earlier draft.

Same I7 class as live snake v3 / lets_v511 / lets_v605 / lets_v607. Cite
when a keep argues I5 is clean because remainder-sheet identifiers are gone.
