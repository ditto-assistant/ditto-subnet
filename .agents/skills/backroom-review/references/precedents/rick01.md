---
id: rick01
agent_id: eefd061d-bc7f-4703-95dd-1d38b1c3f6e7
agent_name: rick01
agent_version: 1
resolution: reject
tags: [family-compiler, production-engine, i5, i3, i7, independent-invariants]
holding: I5 closed value-kind and operand-recipe compiler is fatal even when I4 looks like an untrusted calculator candidate. Do not park as mixed.
---

Never-reviewed raw leader. Passed screening as `deferred-mechanical-admission`
under a policy-v10 stamp; that stamp is not a Luna/L2 review. SHA
`37b86406…`.

I5 is the ground even if the model writes the string. `_VALUE_KINDS`
(`record_selection.py:126`) is a closed address/amount/saying/duration
registry. `EXPRESSION_REQUEST` (`memory_flow.py:91`) injects fixed
balance/remainder/net-effect operand rules: payments and costs subtract,
credits add, corrections replace, named-event subsets use fixed signs, minor
units divide by 100.

I4 can pass narrowly: calculator output labelled a candidate, records remain,
model may return a corrected expression. That pass does not park I5.

I3 also fails: `_keep_better` (`memory_flow.py:56`) suppresses parseable
refusals and the closing loop retries refusal text instead of serving first
dissent.

Live ATH `46ef7070` also found I7 catalog replacement via
`suggest_relevant_tools`. Either invariant is fatal. The miss to avoid is
parking because CALC said “You decide which is right” or because `WORKED OUT`
/ remainder-sheet names grepped clean.

Cite when a later fire wants to leave a high scorer because I4/I7 look mixed.
Independent of [alexandros-v12-19.md](alexandros-v12-19.md) (I4 copy authority)
and [recall-v1.md](recall-v1.md) (I7 next-tool).
