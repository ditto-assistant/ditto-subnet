---
id: alexandros-v12-19
agent_id: 2ceba9c7-35c8-4731-9ac7-a79cffdf1391
agent_name: Alexandros-dittobench_v12-19
agent_version: 1
resolution: reject
tags: [i4, i5, i7, copy-authority, family-compiler, measure-off]
holding: Excluding the optional measure/Program solver is a credit, not a keep. WORKED OUT / these lines win, asks_to_reconcile, and the one right call remain on the default binary.
---

Live champion after rick01 / recall-v1 bans. Different UUID and hotkey from
banned Alexandros-v12-17 (`a308bcbc`). Policy-v10 stamp with
`deferred-mechanical-admission`. SHA `3055bdd3…`. ATH `6bc76a88` reject.

Dockerfile builds `dittobench-miner` without `--features measure`. Credit
that: the offline `Program` solver is not in the image. The prohibited
mechanisms stay on the shipped default path.

I4 — `baseline.rs:3532–3602` labels host-resolved values `WORKED OUT FROM
THIS USER'S OWN RECORDS`, tells the model to say it exactly as written, and
that where they disagree these lines win. `:3624–3630` retains the block for
the recap. That is copy/verify authority, not `untrusted_candidate_channel`.
Model-authored `final_text` does not pass I4.

I5 — `asks_to_reconcile` plus `Reader::bind` / `grounding` prescribe closed
draft/approved/adjustment/payment/correction roles, later-value-wins
arithmetic, and mandatory cents rendering however the question words it.
Excluding `measure` does not remove this served family router.

I7 — `planroute::read_route` maps the planning note onto one of three exact
decks; `baseline.rs:2333/:2340` states `the one right call` is
`create_workflow` / `artifacts`.

Same holding as live Alexandros-v12-17 (`a308bcbc`), Alexandros-v12-3-v2
(`e1283090`), Alexandros-v12-3-v1 (`693c240f`), rb-v11-v32 (`f112734b`).
Cite when a keep credits `cfg(measure)` off or a different hotkey of the
same served path. Extends [alexandros-v10.md](alexandros-v10.md), which
rejected the older `try_solve` / `REPLY WITH EXACTLY` compiler.
