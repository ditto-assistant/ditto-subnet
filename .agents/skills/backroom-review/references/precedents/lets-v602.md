---
id: lets-v602
agent_id: 6dff9d67-44f0-4793-be6d-509258068246
agent_name: lets_v602
agent_version: 1
resolution: reject
tags: [family-compiler, production-engine, renamed-tokens, remainder-coach]
holding: Same-owner restack of banned lets_600; remainder and later-net sheets still decide /run under new prompt names.
---

`LINKED_OUTCOME_SIGN_AUDIT_PROMPT` / `SUMMARY_AUDIT` / `NET_CHANGE_AUDIT`
names were gone. Served `/run` still shipped
`LINKED_RUNNING_QUANTITY_DRAFT_PROMPT` as the graded slot and skipped
the rebuild coach. `_NET_CHANGE_QUERY_RE` is a finite bench-phrase table
that routes to `LINKED_SCOPED_CALCULATION_AUDIT_PROMPT` (`increase;
magnitude` / `decrease; magnitude`). Payment-kept-unchanged remainder
recipe remains.

Renaming SIGN_AUDIT / NET_CHANGE to RUNNING_QUANTITY / SCOPED /
MULTI_OUTPUT does not pass. Inverse of lets_v601 (`a4addb2b`), whose
audit coaches did not compile those amount sheets.
