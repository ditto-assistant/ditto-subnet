---
id: identity-uuid-not-sha
agent_id: 0f433935-2192-408c-a083-03b2b3ff24e4
agent_name: lets_4.0
agent_version: 1
resolution: leave
tags: [identity, uuid, sha, treadmill]
holding: Ban is per agent UUID. Same SHA or same hotkey is a different row.
---

lets_4.0 (`0f433935-…`) stayed `scored` after lets_5.3 / 4.1 were banned.
Its SHA `4124379e…` matches banned lets_4.1, and the hotkey is the same
`5ECg59…` miner. ATH still attaches to one UUID.

After a ban, an older same-hotkey upload can pop back onto the KOTH board
(Athena treadmill). That is not the ban rolling back and it is not a
reason to un-ban. Review the visible UUID on its own source if it is
scored and high-rank.
