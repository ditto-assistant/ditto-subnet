# Private corpus boundary

Production corpus bytes do not belong in this public repository.

Keep private task manifests, upstream identifiers, gold patches, hidden tests,
memory blueprints, encryption keys, and grader bundles in the protected corpus
store. Only immutable commitments, public keys, schemas, and retired audit
artifacts may cross into the public tree after review.
