# Pylon weight-setting service for one SN118 validator. See docs/VALIDATOR.md,
# "Set up Pylon". From the repository root, copy .env.example to
# scripts/pylon/pylon.env, fill it in (never commit it), then:
#   docker compose -f scripts/pylon/docker-compose.yaml up -d
services:
  pylon:
    image: docker.io/backenddevelopersltd/bittensor-pylon:latest # pin to a released tag in production
    restart: unless-stopped
    env_file: ./pylon.env
    ports:
      - "8000:8000"
    volumes:
      # Pylon reads the validator hotkey to sign put_weights; read-only is enough.
      - ~/.bittensor/wallets/:/root/.bittensor/wallets:ro
      # Persist in-flight weight submissions so a restart resumes them.
      - pylon-data:/data

volumes:
  pylon-data:
