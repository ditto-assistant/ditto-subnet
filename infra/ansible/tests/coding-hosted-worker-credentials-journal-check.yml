---
- name: Prove the native worker credential log spot-check stays disabled by default
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  roles:
    - coding_hosted_worker_credentials_journal_check
