---
- name: Prove native worker credential removal stays disabled by default
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  roles:
    - coding_hosted_worker_credentials_cleanup
